% !TEX root=../../report.tex

\Chapter[di]{Dependency Injection und Interception}
Sowohl \gls{asp} als auch Angular wurden mit dem Hintergrundgedanken entwickelt, \gls{di} zu unterstützen. In diesem Kapitel werden die Konzepte der \gls{di} (\myref{sec:di_general}) wie auch der Interception, welche auf \gls{di} aufbaut, beschrieben (\myref{sec:interception_general}). Danach wird gezeigt, wie AutoFac's \gls{di} in C\# funktioniert (\myref{sec:aspDi}) und wie die Interception angewendet wurde (\myref{sec:aspInterception}).
