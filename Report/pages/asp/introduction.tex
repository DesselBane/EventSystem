% !TEX root=../../report.tex

\Chapter[aspcore]{ASP Core}
Eine der Anforderungen war es, dass das EventManager System auf Windows, Linux, sowie Mac läuft. Um dies zu realisieren, wurde das \gls{asp} Framework benutzt. In diesem Kapitel wird beschrieben, wie \gls{asp} als Framework funktioniert (\myref{sec:asp_basics}) und welche Anpassungen stattgefunden haben (\myref{sec:asp_anpassungen}). Es wird gezeigt, welche Architektur genutzt wurde, um das EventManger System möglichst erweiterbar, flexibel, sowie testbar zu machen (\myref{sec:architektur}). Da Front- und Backend nicht von den gleichen Entwicklern erstellt wurden, wird in \myref{sec:swagger} gezeigt, wie die Web \gls{api} dokumentiert wurde.
